! This is the first inlist file that MESA reads when it starts.

! This file tells MESA to go look elsewhere for its configuration
! info. This makes changing between different inlists easier, by
! allowing you to easily change the name of the file that gets read.

&star_job
   load_saved_model = .true.
   load_model_filename = 'planet_create.mod'
   set_initial_age = .true.
   initial_age = 0e0
   set_initial_cumulative_energy_error = .true.
   new_cumulative_energy_error = 0d0

   save_model_when_terminate = .true.
   required_termination_code_string(:) = 'max_age'
   save_model_filename = 'planet_evolve_clouds.mod'

   write_profile_when_terminate = .true.
   filename_for_profile_when_terminate = 'profile_evolve_clouds.data'

   read_extra_star_job_inlist(1) = .true.
   extra_star_job_inlist_name(1) = 'inlist_shared'
/ ! end of star_job namelist


&eos
   read_extra_eos_inlist(1) = .true.
   extra_eos_inlist_name(1) = 'inlist_shared'
/ ! end of eos namelist


&kap
   kap_logical_ctrl(1) = .false.  ! disable grain opacity
   kap_logical_ctrl(2) = .true.  !  enable cloud opacity
   kap_ctrl(1) = 1d-1  ! scaling factor for grain opacity
   kap_ctrl(2) = 1d2  ! ramp-up time for the grain opacity
   kap_ctrl(3) = 1d0  ! cloud-opacity normalisation
   kap_ctrl(4) = 1d2  ! cloud-deck width
   kap_ctrl(5) = 1d6  ! cloud-deck location
   kap_ctrl(6) = 1d1  ! ramp-up time for the cloud opacity
   kap_ctrl(7) = 1.0d0  ! scaling factor for the opacity window
   read_extra_kap_inlist(1) = .true.
   extra_kap_inlist_name(1) = 'inlist_shared'
/ ! end of kap namelist


&controls
   ! stopping
   max_age = 5d9
   ! opacity
   use_other_kap = .true.
   use_other_opacity_factor = .false.
   ! output
   profile_data_prefix = 'profile_evolve_clouds'
   star_history_name = 'history_evolve_clouds.data'
   read_extra_controls_inlist(1) = .true.
   extra_controls_inlist_name(1) = 'inlist_shared'
/ ! end of controls namelist


&pgstar
   read_extra_pgstar_inlist(1) = .true.
   extra_pgstar_inlist_name(1) = 'inlist_pgstar'
/ ! end of pgstar namelist
